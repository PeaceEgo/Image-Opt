{
  "name": "whatsapp-status-optimize",
  "private": true,
  "version": "0.1.0",
  "description": "Prepare photos & videos for WhatsApp Status — web app (photos client-side; videos via external API).",
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "dev": "npm run dev -w web",
    "dev:web": "npm run dev -w web",
    "build": "npm run build -w web",
    "build:web": "npm run build -w web",
    "lint": "npm run lint -w web",
    "typecheck": "npm run typecheck -w web",
    "test": "npm run test -w web"
  },
  "engines": {
    "node": ">=20"
  }
}
